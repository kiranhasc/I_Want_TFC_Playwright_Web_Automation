pipeline {

    agent any

    stages {

        stage('Checkout') {
            steps {
                echo "Checking out repository"
            }
        }

        stage('Build') {
            steps {
                echo "Building application"
            }
        }

    }

}
